import { ImageSourceDirective } from './image-source.directive';

describe('ImageSourceDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageSourceDirective();
    expect(directive).toBeTruthy();
  });
});
